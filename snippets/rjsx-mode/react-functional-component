# -*- mode: snippet -*-
# name: react-functional-component
# key: rfc
# --
import React from 'react'
import PropTypes from 'prop-types'

const $1 = ({}) => {

  return (
    $0
  )
}

$1.propTypes = {}

export default $1